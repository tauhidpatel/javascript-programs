// simple brute force linear solution

function linearSearch(arr) {
    let i = 2 ;
    for(let num of arr) {
        if(i == 2) {
            return true;
        }
            return false;
    }
}

const res = linearSearch([1, 2, 3, 6, 8]);
console.log(res);
